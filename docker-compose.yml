services:
  mongo:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
  server:
    build:
      context: .
      dockerfile: docker/server.Dockerfile
    env_file:
      - .env.example
    ports:
      - '4000:4000'
    depends_on:
      - mongo
  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    env_file:
      - .env.example
    ports:
      - '3000:3000'
    depends_on:
      - server
volumes:
  mongo-data:
